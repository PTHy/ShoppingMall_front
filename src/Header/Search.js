import React from 'react';

const Search = (props) => {
    return (
        <div className="search-container">
            <span>Power Search</span>
            <input type="text"/>
            <button>검색</button>
        </div>
    );
};

export default Search;